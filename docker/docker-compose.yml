services:
  mongodb:
    image: mongo:7
    container_name: weather_mongo
    restart: always
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=weather_service
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: